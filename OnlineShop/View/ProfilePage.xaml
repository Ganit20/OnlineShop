<Page x:Class="OnlineShop.View.ProfilePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:OnlineShop.View"
      xmlns:model="clr-namespace:OnlineShop"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProfilePage" FontSize="30">

    <Grid>
        <Grid HorizontalAlignment="Center"  VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel>
                <TextBlock Text="{Binding Path=(model:StaticInfo.UserName) }"/>
                <TextBlock Text="{Binding Path=(model:StaticInfo.UserEmail) }"/>
                <TextBlock Text="{Binding Path=(model:StaticInfo.UserRole) }"/>
            </StackPanel>

        </Grid>
        
    </Grid>
</Page>
